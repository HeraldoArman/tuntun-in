# Public, non-secret Convex URLs — baked into the client bundle at build time.
# Committed (not gitignored) so CI and Docker builds have these available
# without external secret configuration. Secrets stay in gitignored .env.
NEXT_PUBLIC_CONVEX_URL=https://beaming-jackal-306.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://beaming-jackal-306.convex.site
